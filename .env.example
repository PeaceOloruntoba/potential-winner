# Backend API base URL (separate repo/deployment). Local dev default matches
# the backend's `npm run dev` port. In production this points at the
# Vercel deployment (demo) or the school's own VPS domain.
VITE_API_BASE_URL=http://localhost:5000/api/v1
